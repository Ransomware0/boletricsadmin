/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    domains: ['home-statics.boletia.com'],
    unoptimized: true,
  },
};

module.exports = nextConfig;
